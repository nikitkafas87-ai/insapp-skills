interface:
  display_name: "OSAGO Insurers Check"
  short_description: "Check OSAGO insurer connectivity"
  default_prompt: "Use $osago-insurers-check to audit OSAGO insurer connectivity for active partner ApiKeys and generate the HTML matrix report."
policy:
  allow_implicit_invocation: true
